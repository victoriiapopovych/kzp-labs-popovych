# Звіт до лабораторної роботи №1

## 1. Тема, номер роботи та варіант

Лабораторна робота №1.

Тема: «Розгортання Java-проєкту та базова обробка даних».

Предметна область: метеостанція.

Варіант: 18.

Операційна система: Windows.

## 2. Мета роботи

Мета роботи — створити базову версію консольної Java-програми та підготувати структуру репозиторію для її подальшого розвитку.

На першому рівні реалізовано читання текстових даних, перевірку коректності записів, базові числові обчислення, форматований вивід результатів у консоль і текстовий файл.

Для варіанта 18 «Метеостанція» програма обробляє погодні спостереження та визначає кількість коректних записів, мінімальну температуру, середню вологість і найбільшу швидкість вітру.

## 3. Постановка задачі

Потрібно розробити консольну Java-програму для обробки даних метеостанції.

Один запис має формат:

`date;temperature;humidity;pressure;wind`

Поля запису:
- `date` — дата спостереження, `String`;
- `temperature` — температура, `double`;
- `humidity` — вологість, `double`;
- `pressure` — атмосферний тиск, `double`;
- `wind` — швидкість вітру, `double`.

Програма повинна:
1. прочитати записи з UTF-8 файла;
2. перевірити кожний рядок;
3. повідомити номер і причину пропуску некоректного рядка;
4. не враховувати хибні записи під час обчислень;
5. визначити кількість коректних записів;
6. знайти мінімальну температуру;
7. обчислити середню вологість;
8. знайти найбільшу швидкість вітру;
9. сформувати один звіт;
10. вивести звіт у консоль і записати його у файл.

## 4. Структура програми

Після виконання третього рівня проєкт має таку основну структуру:

```text
kzp-labs-popovych/
├── .github/
│   └── workflows/
│       └── ci.yml
├── .mvn/
│   └── wrapper/
│       └── maven-wrapper.properties
├── ai/
│   ├── manager.md
│   └── devops.md
├── data/
│   └── input.csv
├── src/
│   ├── main/
│   │   └── java/
│   │       └── ua/
│   │           └── lpnu/
│   │               └── kzp/
│   │                   └── Main.java
│   └── test/
│       └── java/
│           └── ua/
│               └── lpnu/
│                   └── kzp/
│                       └── MainTest.java
├── .editorconfig
├── .gitattributes
├── .gitignore
├── mvnw
├── mvnw.cmd
├── pom.xml
├── README.md
└── REPORT.md
```

Файл `Main.java` містить консольну програму для обробки записів метеостанції.

Файл `MainTest.java` містить JUnit 5 тести для перевірки коректних, помилкових і граничних сценаріїв.

Файл `pom.xml` описує Maven-проєкт, залежності, SpotBugs і створення виконуваного JAR.

Файли `mvnw`, `mvnw.cmd` та каталог `.mvn/wrapper/` утворюють Maven Wrapper.

Файл `.github/workflows/ci.yml` описує GitHub Actions workflow для автоматичної перевірки проєкту на Ubuntu, Windows і macOS.

Каталог `ai/` містить описи ролей Менеджера та DevOps, які використовувалися під час виконання роботи.

Каталоги `target/` та `out/` не додаються до Git, оскільки містять результати, які можна автоматично відтворити під час збірки або запуску програми.

## 5. Інфраструктура проєкту

На першому рівні було створено базову структуру Java-проєкту, Git-репозиторій і консольну програму.

У проєкті використовуються:
- `.editorconfig` — для однакового форматування файлів;
- `.gitattributes` — для нормалізації текстових файлів;
- `.gitignore` — для виключення з Git згенерованих каталогів і файлів.

Вхідні та вихідні текстові файли обробляються у кодуванні UTF-8, а для шляхів використовується `Path`.

На другому рівні проєкт переведено на Maven. У `pom.xml` налаштовано:
- `groupId` — `ua.lpnu.kzp`;
- `artifactId` — `lab01`;
- версію — `1.0.0`;
- Java 21;
- UTF-8;
- JUnit Jupiter 5.12.2;
- SpotBugs;
- Maven Shade Plugin для створення виконуваного JAR.

Додано Maven Wrapper 3.3.4, який використовує Maven 3.9.11.

Локально успішно виконано:

```powershell
.\mvnw.cmd test
```

Результат: 9 тестів, 0 failures, 0 errors, 0 skipped.

Також успішно виконано:

```powershell
.\mvnw.cmd verify
```

Під час `verify` запускаються JUnit 5 тести та SpotBugs. SpotBugs не виявив дефектів.

Створення виконуваного JAR перевірено командою:

```powershell
.\mvnw.cmd package
```

Результатом є:

```text
target/lab01-1.0.0.jar
```

Запуск JAR:

```powershell
java -jar target/lab01-1.0.0.jar
```

Перевірка довідки:

```powershell
java -jar target/lab01-1.0.0.jar --help
```

Перевірка версії:

```powershell
java -jar target/lab01-1.0.0.jar --version
```

Результат:

```text
1.0.0
```

На третьому рівні додано GitHub Actions workflow.

CI запускається під час `push` і `pull_request` та виконує `verify` на:
- Ubuntu;
- Windows;
- macOS.

На кожній операційній системі використовується Temurin JDK 21 та Maven Wrapper.

Усі три CI jobs завершуються успішно.

Після успішної збірки JAR публікується як GitHub Actions artifact окремо для кожної операційної системи.

## 6. GitHub Issues і Pull Request

Для лабораторної роботи створено GitHub Issues.

Issue #1 — «Налаштування лабораторної роботи №1».

У ньому було зафіксовано мету та критерії готовності першого рівня.

Issue #2 — «Налаштувати Maven-проєкт».

Критерії:
- додати `pom.xml`;
- налаштувати Java 21 та UTF-8;
- додати Maven Wrapper;
- підключити JUnit 5;
- успішно виконати `mvnw.cmd test`.

Issue #3 — «Налаштувати аналізатор і пакування».

Критерії:
- підключити SpotBugs;
- запускати SpotBugs у фазі `verify`;
- успішно виконати `mvnw.cmd verify`;
- налаштувати Maven Shade Plugin;
- створити виконуваний JAR;
- перевірити запуск JAR через `java -jar`.

Для Issues #2 і #3 використано мітку `infra`.

Issue #4 — «Налаштувати GitHub Actions».

Критерії:
- додати GitHub Actions workflow;
- перевіряти проєкт на Ubuntu, Windows і macOS;
- використовувати Java 21;
- запускати Maven `verify`;
- запускати JUnit 5 тести на всіх трьох ОС;
- публікувати JAR як artifact.

Робота над лабораторною роботою виконувалася у гілці `develop-lab1`.

Створено Pull Request #5 «Lab 1: Meteostation» з гілки `develop-lab1` у `develop`.

У Pull Request наведено короткий опис реалізованих змін, команди перевірки та стан CI.

Перевірки Pull Request успішно виконано на Ubuntu, Windows і macOS.

Pull Request пов'язано з відповідними Issues:

`Closes #1`

`Closes #2`

`Closes #3`

`Closes #4`

## 7. Приклади роботи програми

Приклад вхідних даних:

```text
2026-09-01;18.5;62.0;1013.2;3.4
2026-09-02;-2.0;80.0;1005.0;7.8
2026-09-03;25.0;45.0;1018.6;1.2

2026-09-04;помилка;70.0;1010.0;4.0
2026-09-05;20.0;-5.0;1012.0;3.0
2026-09-06;10.0;50.0;1000.0
```

Для некоректних записів програма виводить номер рядка та причину пропуску.

Приклад:

```text
Пропущено рядок 4: порожній рядок
Пропущено рядок 5: температура, вологість, тиск і вітер повинні бути числами
Пропущено рядок 6: вологість не може бути від'ємною
Пропущено рядок 7: очікується 5 полів, отримано 4
```

Для наведеного набору даних отримано:

```text
Метеостанція — варіант 18
Коректних записів: 3
Мінімальна температура: -2.00
Середня вологість: 62.33
Найбільша швидкість вітру: 7.80
```

Той самий звіт записується у файл:

```text
out/report.txt
```

Команда:

```powershell
java -jar target/lab01-1.0.0.jar --version
```

виводить:

```text
1.0.0
```

## 8. Тестування

Для перевірки програми використано JUnit 5.

Реалізовано 9 тестів.

Перевіряються:
1. коректний рядок;
2. порожній рядок;
3. неправильна кількість полів;
4. нечислове числове поле;
5. від'ємне значення там, де воно недопустиме;
6. файл, у якому всі записи некоректні;
7. правильне середнє для кількох записів;
8. українські символи та UTF-8;
9. аргумент `--version`.

Локальний запуск:

```powershell
.\mvnw.cmd test
```

Результат:

```text
Tests run: 9, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

Повна перевірка:

```powershell
.\mvnw.cmd verify
```

SpotBugs завершив перевірку без знайдених дефектів.

У GitHub Actions ті самі тести успішно виконуються на:
- Ubuntu — успішно;
- Windows — успішно;
- macOS — успішно.

Посилання на успішний запуск CI:

`<https://github.com/victoriiapopovych/kzp-labs-popovych/actions/runs/35454421635>`

Посилання на JAR artifact:

`<https://github.com/victoriiapopovych/kzp-labs-popovych/actions/runs/35454421635>`

## 9. Документація

У програмі використано Javadoc.

Документовано клас `Main`, точку входу `main` та нетривіальні допоміжні методи.

Генерацію Javadoc було перевірено локально.

README містить:
- призначення програми;
- формат вхідних даних;
- приклад запису;
- команди запуску;
- Maven-команди;
- запуск JAR;
- `--help`;
- `--version`;
- опис GitHub Actions CI.

## 10. Академічна доброчесність

Під час виконання лабораторної роботи використовувався генеративний штучний інтелект ChatGPT.

ШІ використовувався у спеціалізованих ролях:
- системний архітектор / Менеджер — для аналізу методичних вимог, планування етапів та формування критеріїв готовності;
- Java-розробник — для підготовки та перевірки Java-коду і JUnit-тестів;
- QA / Валідатор — для перевірки крайових випадків і результатів тестування;
- DevOps — для Maven, Wrapper, SpotBugs, JAR і GitHub Actions.

Окремими файлами в репозиторії задокументовано дві базові ролі:
- `ai/manager.md`;
- `ai/devops.md`.

Java-розробник і QA / Валідатор використовувалися як спеціалізовані ролі під час роботи над кодом і тестуванням, але окремі файли для них у репозиторії не створювалися.

Приклади запитів до ШІ:
- розкласти вимоги Level 1–3 на послідовні етапи;
- перевірити `pom.xml`;
- налаштувати Maven Wrapper;
- перевірити JUnit 5 тести;
- налаштувати GitHub Actions на Windows, Ubuntu та macOS;
- перевірити відповідність README і REPORT методичним вимогам.

Було прийнято рекомендації щодо Maven, UTF-8, JUnit 5, SpotBugs, Maven Shade Plugin, GitHub Actions, структури GitHub Issues і документації.

Під час роботи було виправлено:
- проблему з відображенням UTF-8 у PowerShell;
- проблему з доступністю `javadoc.exe`;
- помилку під час генерації Maven Wrapper;
- права виконання `mvnw` для Unix;
- розташування workflow у `.github/workflows/`;
- застарілі версії GitHub Actions;
- тест файла без коректних записів.

Усі запропоновані рішення перевірялися локальними командами та GitHub Actions.

Автор розуміє призначення використаних конструкцій та може пояснити поданий код.

## 11. Відповіді на контрольні питання

1. `pom.xml` описує Maven-проєкт, його координати, залежності, версію Java та плагіни.

2. Фаза `test` запускає тести, `verify` виконує перевірки до завершення життєвого циклу перевірки, а `package` створює пакет програми, у цьому проєкті — JAR.

3. Maven Wrapper дозволяє використовувати зафіксовану версію Maven без необхідності окремо встановлювати однаковий Maven на кожному комп'ютері.

4. Метод `main` є точкою входу консольної Java-програми.

5. Примітивні типи зберігають прості значення. `String` є посилальним типом і представляє об'єкт-рядок.

6. Середнє обчислюється як `double`, щоб не втратити дробову частину.

7. Якщо метод перетворення числа отримує текст, який не можна перетворити, виникає `NumberFormatException`.

8. Помилковий рядок не можна пропускати без повідомлення, оскільки користувач повинен знати, які дані не потрапили до обчислень.

9. `split(";", -1)` зберігає порожнє останнє поле.

10. `Path.of` дозволяє працювати зі шляхами незалежно від конкретної операційної системи.

11. Явне UTF-8 забезпечує однакове кодування текстових файлів на різних ОС.

12. `%n` створює системно коректний роздільник рядків.

13. Для варіанта 18 перевіряються кількість полів, дата, числові значення та недопустимі від'ємні значення. Від'ємна температура є допустимою.

14. Коректність середнього перевіряється тестом із кількома значеннями, для яких очікуваний результат відомий заздалегідь.

15. Статичний аналізатор перевіряє програму на потенційні дефекти без звичайного сценарію її виконання.

16. GitHub Actions автоматично запускає перевірки після `push` і `pull_request` та дозволяє виконати ті самі тести на різних операційних системах.

17. GitHub Issue для дефекту має містити опис проблеми, кроки відтворення, очікувану і фактичну поведінку.

18. У розділі академічної доброчесності потрібно зафіксувати інструменти ШІ, їхні ролі, запити, прийняті рекомендації, виправлені помилки та власний внесок.

19. DevOps відповідає за інфраструктуру збірки й CI, а Валідатор — за перевірку поведінки та крайових випадків.

## 12. Висновки

У лабораторній роботі створено консольний Java-застосунок для предметної області «Метеостанція».

Програма читає UTF-8 файл, перевіряє записи, повідомляє про некоректні рядки та не враховує їх під час обчислень.

Для коректних записів визначаються кількість записів, мінімальна температура, середня вологість і найбільша швидкість вітру.

Проєкт переведено на Maven, додано Maven Wrapper, JUnit 5, SpotBugs та створення виконуваного JAR.

Реалізовано 9 автоматизованих тестів.

GitHub Actions запускає перевірки на Windows, macOS та Ubuntu, а JAR публікується як artifact.

Версія програми — `1.0.0` та доступна через параметр `--version`.

Для версії програми `1.0.0` використовується Git-тег `v1.0.0`.

Створено фінальний Pull Request #5 «Lab 1: Meteostation», перевірки якого успішно пройшли на Ubuntu, Windows і macOS.

Створена інфраструктура та зовнішня поведінка програми стануть основою лабораторної роботи №2.